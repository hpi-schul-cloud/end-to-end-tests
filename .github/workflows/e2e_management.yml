---
name: e2e management

on:
  push:
    branches-ignore:
      - dependabot/**

jobs:
  tag-managment:
    uses: hpi-schul-cloud/end-to-end-tests/.github/workflows/e2e.yml@BC-6654-update-mongodb
    with:
      ref: ${{ github.ref }}
      e2e_tag: management

